# Epic 3 · 3.2 — záró jelentés (Zóna-tartalom + nevezék + elavulás-jelölők + lezárt fázis)

**Dátum:** 2026-07-22 · **Branch:** `dev` · **Spec:** Refounded_Epic3_32_spec v0.1 (Approved) — épül a 3.1-re (Csomag B1)
**Verifikáció:** **MOCK_LLM** (MOCK_LLM=1) — nem valós LLM-mel. A valós-LLM ellenőrzés az Epic-végi közös sign-off része.
**Commitok:** `…epic3-32-compliance` (#0) → `…zóna-nevezék + D1/D2/D3 infra` (#1) → `…②/③ tartalom` (#2) → `…katalógus-láthatóság` (#3) → `…lezárt fázis` (#4) → e jelentés (#5).

---

## 1. Mi készült el

- **3.2-a · Zóna-nevezék (i18n):** ① „Bemenet"→**„Források"**, ② „Munkaeszközök"→**„Feldolgozás"**, ③ Kimenet és ④ Kapu változatlan. Megjelenítési nevek egységesítve: **Use case-rangsor** (volt: Priorizált use case-shortlist), **Megoldásterv** (volt: Megoldás-dokumentáció felirat), **Kiértékelési riport** (volt: Tesztriport felirat) — a `typeDef.key`/DB-érték változatlan, csak a HU/EN megjelenítési label módosult. Új `artifacts.gateDeliverableBadge` = „kapu-deliverable" (kiírva, nem csak ikon). D1/D2/D3-jelzés infrastruktúra: `docTypeOf(typeDef)` klasszifikátor (`src/lib/artifacts/config.ts`), tisztán a típus tulajdonságaiból derivált (nincs fázis-specifikus hardcode).
- **3.2-b · ② Feldolgozás tartalma:** P0 = 3 field-extract blokk (Projekt-charter, Engagement-terv, Kickoff-agenda); P1 = 3 entitás-gyártó (Fájdalompontok, Use case-ek, Stakeholderek) + 1 field-extract (Felmérési riport); P2 = 2 field-extract (Business case, Pilot-terv) — a Megoldási javaslat (D2, entitásforrású) **nem** jelenik meg itt; P3 = **üres-állapot**, a toolokra mutató szöveggel. A ②-ben dokumentum-kártya sehol nem jelenik meg — `d1Types = phaseTypes.filter(td => docTypeOf(td) === "D1")` váltotta az elavult `!entitySourced` szűrést (ez javította a P2/P3 hibás besorolást is, lásd 3. pont).
- **3.2-c · ③ Kimenet tartalma:** minden fázis dokumentumai D1/D2/D3 típusjelzéssel + „kapu-deliverable" jelvénnyel (ahol `typeDef.gate`), teljes funkcióval (generálás/kivonatolás, szerkesztő-link, státusz, verzió-lista, export). A P3 D3-dokumentumok (Megoldásterv, Kiértékelési riport) a ③-kártyán belül kapják a kivonatolás-indítást + a 🔒(modul-mező)/✎(doc-mező) partíciós jelzést — a meglévő `ExtractForm`+`FieldCard` gépezet áthelyezve, **nem** új logika. A P2 ③ zóna-kártya számlálója (a compliance-check által talált hiba) javítva: unconditional dokumentum-számláló, azonos mintában a többi zónával.
- **3.2-d · Katalógus-láthatóság:** ⟳ elavulás-jelölő (`StaleFlag`, a Csomag A 2.1 jelölő-rétegéből — `doc_stale`/`render_stale` a `docStaleSinceForArtifact`+`renderStaleSinceForArtifact` függvényekkel, `activeStaleSince`-en át ack-szűrve) minden ③-kártyán, ahol indokolt; „hol van használva" darabszám-badge (`usageOf()` a Csomag C1 katalógus-rétegből) a megerősített P1-entitásokon (fájdalompont/use case/stakeholder), üres használatnál explicit „még nincs használva". A 2.1/C1 réteg **fogyasztva**, nem újraírva — ez volt a compliance-check fő ellentmondás-kockázata (korábban egyik rétegnek sem volt UI-fogyasztója).
- **3.2-e · Lezárt fázis olvasható állapota:** `PhaseWorkspace` új `locked` prop + `lockWrap()` helper (pointer-events-none + halványítás + `aria-disabled`) — a ② teljes tartalma, az entitás-generátor gombok és a D3 ③-kártyákba ágyazott kivonatolás inaktív, de LÁTHATÓ; a navigációs linkek (szerkesztő megnyitása, verzió-előzmény) a wrapperen KÍVÜL maradnak, ezért aktívak. „Lezárva" jelvény az összegző sávban. `GateCloseForm` nem renderelődik lezárt fázison. A fázis-oldal `completed` ága mostantól a teljes zóna-vázas `PhaseWorkspace`-t is rendereli `locked`-dal (a tool-sáv és a ③ ugyanúgy elérhető, mint nyitott fázison) — korábban a completed nézet a zóna-vázat egyáltalán nem mutatta. Visszanyitás a felületen nincs (nincs is ilyen action a kódbázisban — `PIVOT_LOOP_ENABLED = false`).

## 2. Döntések (a compliance-check keretében, a scope-on belül)

- **D3-extrakció relokációja ③-ba:** a spec egyszerre kéri a P3 ② ürességét ÉS azt, hogy a kapu-kritikus `architektura` mező AI-kivonatolható maradjon. A `docTypeOf` klasszifikáció + a `FieldWorkBody`/`FieldWorkBlock` szétválasztása oldja fel: a meglévő extrakciós UI a D3-típusú ③-kártyába költözik, a spec F3 „teljes funkcióval (generálás vagy kivonatolás)" pontja alapján — nem új logika, csak áthelyezés.
- **P2 zóna-kártya számláló bugfix:** a compliance-check feltárta, hogy a régi metrika (`outRequired > 0 ? … : undefined`) az egyetlen zóna-kártya volt, amely 0-nál eltűnhetett; a többi zóna mindig mutat számot. Unconditional dokumentum-számlálóra cserélve.
- **„entitás→tudáselem" átnevezés köre:** a spec bare szava csak az ÚJ 3.2-d szövegekben (usage badge-ek) került bevezetésre; a P1 UI meglévő „entitás" szóhasználata NEM lett átírva — nincs rá acceptance criteria, és jelentős, kontrollálatlan scope-bővítés lenne.
- **Stale nav-labelek (`nav.workbench`/`nav.heatmap`):** a globális `ProjectContextNav.tsx` sidebarban talált, B1 előtti angol maradvány-feliratok az „Érintett fájlok" listán KÍVÜL esnek (nem a zóna-panelek/i18n-kulcsok/tudáselem-kártyák köre) — megfigyelt, de érintetlenül hagyott lelet, dokumentálva a compliance jelentésben.

## 3. Verifikáció — MOCK_LLM, lokális stack (PG16 + PostgREST-shim + prod build)

| Réteg | Eredmény |
|---|---|
| `npx tsc --noEmit` | ✓ |
| `npm run build` | ✓ |
| `npm run i18n:check` | ✓ (1787 kulcs, HU/EN paritás) |
| UI-walkthrough (Playwright, P0–P3, 2× lefuttatva idempotencia-ellenőrzéssel) | **44/44** mindkét futáson |

**UI-forgatókönyvek (44/44):** zóna-fülek felirata FORRÁSOK/FELDOLGOZÁS (a régi BEMENET/MUNKAESZKÖZÖK sehol) · P0 ②-ben a 3 field-extract cím, dokumentum-kártya NINCS · P1 ②-ben 3 entitás-gyártó + Felmérési riport, valós kivonatolás (E1-lánc regresszió is: 3 javaslat-kártya megjelenik) · P2 ②-ben Business case + Pilot-terv, Megoldási javaslat KIMARAD · P3 ② üres-állapot, kivonatolás-gomb NINCS a ②-ben · D1/D2/D3 jelvények mind a 4 fázison + „kapu-deliverable" a shortliston · régi „Priorizált use case-shortlist" felirat sehol, „Use case-rangsor" mindenhol · P2 ③ zóna-kártya számlálója látható (bugfix igazolva) · P3 D3-kártyán 🔒/✎ jelzés + BEÁGYAZOTT kivonatolás-indítás · „hol van használva" badge megjelenik megerősített fájdalomponton · **doc_stale** StaleFlag ténylegesen megjelenik egy P3 Megoldás-dokumentáció draft-on, miután egy hozzá tartozó build_component a `synced_at` UTÁN frissült · „Lezárva" jelvény a lezárt (P0, seed-ből completed) fázison · a ② mutáló blokkja `pointer-events-none`, de a tartalma LÁTHATÓ · a ③ „Szerkesztő megnyitása" link AKTÍV marad, ténylegesen megnyitható (200-as válasz) lezárt fázison · `GateCloseForm` nem renderelődik lezárt fázison · nincs „Visszanyitás"/„Újranyitás" akció sehol · NF2 — mind a 4 fázison (P0–P3) mind a 4 zóna-fül jelen · NF1 regresszió — a B1 tool-sáv fejléce változatlanul jelen. 0 böngésző-JS-hiba a teljes futás alatt.

## 4. „Kész, ha" — pontonként (F1–F6 + NF1–NF2)

- ✅ **F1 (3.2-a):** a régi zóna-feliratok sehol, az újak minden fázison; egységesített dokumentum-nevek; „kapu-deliverable" kiírva.
- ✅ **F2 (3.2-b):** ② tartalma fázisonként a spec szerint (P0:3 · P1:3+1 · P2:2 · P3:0+toolokra mutató üres-állapot); dokumentum-kártya sehol a ②-ben.
- ✅ **F3 (3.2-c):** ③-ban kizárólag a fázis dokumentumai, D1/D2/D3 jelzéssel, teljes funkcióval; P3 mező-partíció (🔒/✎) a meglévő D3-mechanizmussal.
- ✅ **F4 (3.2-d):** elavulás-jelölő ott, ahol a 2.1 réteg indokolja (doc_stale élesben igazolva); usage-badge megerősített tudáselemeken, üres esetben explicit szöveggel.
- ✅ **F5 (3.2-e):** „Lezárva" jelölő; ② mutáló triggerei inaktívak, de láthatók; toolok + ③ dokumentumai lezárt fázison is megnyithatók; visszanyitás nincs a felületen.
- ✅ **NF1:** nincs regresszió — a B1 váz (tool-sáv, zóna-váltás, kapu-navigáció), a Draft→Approved lánc, a kapu-zárás és a generálás változatlanul működik.
- ✅ **NF2:** fázis-paritás — P0–P3 azonos zóna-váz (4/4 fül mindenhol).

## 5. Nem érintett (kényszer)

Séma/entitás-réteg (nincs migráció — a compliance-check nem talált hiányzó adatot, amely STOP-ot indokolt volna). A 3.1 váza (tool-sáv, zóna-váltó mechanizmus, kapu-navigáció) — **fogyasztva**, nem átírva. Tool-előnézetek vizuálja — **3.3**. Stakeholder-mátrix — **3.4**. F4 default-zóna logika — 3.1p (B1) alatt zárult, itt nem módosult. AICON→Refounded kód-szintű rename — külön csomag.

## 6. Parkoló-lista (scope-on kívül)

- A P1 UI meglévő „entitás" szóhasználatának egységesítése „tudáselem"-re — nincs hozzá acceptance criteria, nagy felületű, kontrollálatlan scope-bővítés lenne.
- `ProjectContextNav.tsx` sidebar stale angol feliratai (`nav.workbench`/`nav.heatmap`) — a compliance jelentésben dokumentált, de az „Érintett fájlok" listán kívül eső lelet.
- Billentyűzet-fókusz a `pointer-events-none` wrapperen belül (a lezárt ② gombjai egérrel/érintéssel nem aktiválhatók, de Tab-bal technikailag még fókuszálhatók) — arányos korlátozás e csomag méretéhez, ugyanaz a jellegű határ, mint az artifact-szerkesztő oldal korábban dokumentált korlátja.

*Verifikáció MOCK_LLM-mel futott. A cleanup megtörtént (.env.local törölve, lokális stack leállítva).*
