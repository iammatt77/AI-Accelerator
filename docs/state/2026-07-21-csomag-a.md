# Záró jelentés — Csomag A: Adatmodell-integritás + Verzió/elavulás-réteg

**Dátum:** 2026-07-21 · **Branch:** `dev` · **Spec:** Csomag A v0.2 (Approved) — A1–A6, A8, A9
(az **A7 kötéstábla-konszolidáció NEM része** — külön csomag lesz)

## Commit-lánc

| Commit | Tartalom |
|---|---|
| `2cfd808` | #A/1 — 0013 migráció + config-flagek (moduleOwned, retired) + csoport-alapú citáció-lib (`lib/sources`) |
| `517e832` | #A/2 — D3-partíció: extract-szűrés, approve-sync-őr, synced_at írás, residual state-őr, read-only modul-mezők |
| `a520879` | #A/3 — entitás-primer: suggestBuildDoc + suggestEvalCases, `[index] cím` citációs lista, jóváhagyott-entitás blokkok |
| `03b34b4` | #A/4 — Megoldási javaslat D2-átkötés (A5) + TO-BE terv kivezetés (A6) |
| `dc8f094` | #A/5 — verzió/elavulás-réteg (A8): forrás-verziók, derivált jelölők, StaleFlag + ack |
| `ed2b4f0` | Verifikációs lelet-javítás: seeded_at az új seed/AI-eredetű build-komponenseken |

## Megvalósítás követelményenként

- **A1 (D3-partíció, modul-mezők):** a 6 modul-mező (`solutiondoc`: komponensek / prompt_konyvtar / guardrail_hitl; `testreport`: golden_set_eredmeny / atmenesi_arany / hibak_javitasok) `moduleOwned` flaget kapott. Egyetlen írójuk a modul-szinkron (`syncDocAction` / `syncReportAction` — mindkettő `artifacts.synced_at` bélyeget ír). Az `extract()` kihagyja őket a sémából (mock: null), az `extractAction` merge érintetlenül hagyja őket. A szerkesztőben és a munkaterület ② mező-kártyáin read-only: „a modulból frissül" jelvény + az utolsó sync időpontja; E1-akciók és kézi kitöltés elrejtve. Az approve modul-mezős típusnál `synced_at` nélkül blokkolt (`errors.approveNeedsSync`).
- **A2 (maradek_kockazat = doc-mező):** NEM moduleOwned; a `suggestResidualRiskAction` state-őrrel fut — kézi/megerősített értéket nem ír felül (`goldenset.noticeRiskKept`), `ai_filled`-et frissíthet (E1 marad).
- **A3 (S1-javítás):** `suggestBuildDoc` elsődleges tartalma a JÓVÁHAGYOTT (confirmed/manual) `solution_components` teljes tartalma (leírás + HITL-nyertes opció leírással); a nyers források `[index] cím` citációs listává váltak (suggestRequirements-minta). Jóváhagyott komponens nélkül a hívás blokkolt (`builddoc.errNeedApprovedComponents`); az `origin_index` a szűrt jóváhagyott-listára képez.
- **A4 (S2-javítás):** `suggestEvalCases` elsődleges tartalma a jóváhagyott use case; források `[index] cím` listaként. Nem-jóváhagyott use case-nél blokk (`goldenset.errNeedApprovedUseCase`).
- **A5 (Megoldási javaslat D2-átkötés):** a típus `entitySourced` lett — a nyers kivonatolási út megszűnt (szerver-őr + UI-hint). Új `generateSolutionPlanFromEntitiesAction`: determinisztikus kompozíció a jóváhagyott komponensekből + HITL-nyertes opciókból (megoldás-leírás, opció-összevetés szempont-értékekkel, döntési kritériumok, választott use case); citációk az érintett entitások forrás-uniójából; `synced_at` bélyeg. **Nyertes-szabály:** minden jóváhagyott komponenshez kell kiválasztott opció — különben blokk a hiányzók név szerinti felsorolásával. OutputCard: típus-specifikus generátor-gomb.
- **A6 (TO-BE terv kivezetés):** `retired: true` — a P2 munkaterületen nem jelenik meg (`typesForPhase` szűr), új draft/verzió szerveroldalon is tiltott (`errors.typeRetired` az extract + newVersion útvonalon), a tár `typesForPhaseAll`-lal CSAK akkor mutatja, ha van meglévő sora; `getTypeDef` továbbra is feloldja; adat nem törlődött.
- **A8 (verzió/elavulás-réteg):**
  - `input_items.group_id + version`; a kanonikus `[n]` számozás CSOPORTONKÉNT él (képviselő = legfrissebb verzió, sorrend = az első verzió created_at-ja — stabil). Az `aliasIndex` BÁRMELY verzió-id-t a csoport indexére old fel → a régi citációk nem törnek el.
  - `newSourceVersionAction`: a frissítés ÚJ sor ugyanabban a csoportban (v+1), a régi megmarad. Források oldal: csoport-alapú lista, vN chip, „Korábbi verziók" szekció, új-verzió űrlap.
  - Derivált jelölők (`lib/staleness`, jelölő-állapot NEM tárolódik): `source_updated` (nem-legfrissebb verzió-hivatkozás), `origin_drift` (`solution_component.updated_at > build_components.seeded_at`), `doc_stale` (modul-entitás változás > `artifacts.synced_at`). Csak a nyugta tárolódik (`stale_acks`); a nyugta utáni ÚJABB változás automatikusan újra jelöl.
  - Minimál badge (`StaleFlag`) beépített „Ellenőrizve" akcióval — P1 entitás-részleteken, builddoc soron/részleten, artefaktum-szerkesztő fejlécben.
  - A munkaterület ① listája és a mező-chipek is a kanonikus csoport-számozást használják (vN jelvény; a verzió-emelés nem szaporítja a listát).
- **A9 (regresszió-védelem):** E1-lánc, státuszlánc, kapu-poka-yoke-ok, #14/#15 folyamatok érintetlenek — lásd a verifikációt.

## Verifikáció (lokális PG16 + shim + prod build + MOCK_LLM, Playwright)

Teljes futás: **52/52 zöld** (`walkA.mjs`, screenshotok: a6-1…a6-12). A spec §6 forgatókönyv-térképe:

| §6 forgatókönyv | Eredmény |
|---|---|
| 1. Partíció: kézi tartalom + sync → doc-mezők érintetlenek | ✅ kézi `architektura` a sync után változatlan; modul-mezők frissültek; `synced_at` kitöltve |
| 2. Extract-séma: modul-mezőkre nincs javaslat | ✅ extract után modul-mező érték+state érintetlen; doc-mező (`uzemeltetesi_jegyzet`) `ai_filled` javaslatot kapott |
| 3. S1/S2: entitás-primer + `[index] cím` citáció | ✅ kód-szinten (prompt-összeállítás a2/a3 commitban) + UI: a Megoldási javaslaton nincs nyers extract-út (entitás-hint); blokkok jóváhagyott entitás nélkül |
| 4. Drift: P2-komponens változás → P3-jelölő, tartalom változatlan | ✅ origin_drift jelölő a build-soron; a komponens tartalma változatlan; „Ellenőrizve" után eltűnik |
| 5. Forrás v2 → jelölő | ✅ v+1 chip, korábbi verziók, kanonikus `[1]` változatlan, régi citáció aliason él; source_updated a pain-részleten; ack → eltűnik; ÚJABB verzió → újra jelöl |
| 6. Megoldási javaslat nyertesből | ✅ blokk a nyertes nélküli komponens felsorolásával; nyertessel generálás → confirmed mezők + `synced_at` + nyertes opció a tartalomban |
| 7. TO-BE terv nem hozható létre | ✅ nincs a P2 munkaterületen, nincs a tárban (nincs meglévő sora); szerver-őr (`typeRetired`) az extract/newVersion útvonalon |
| 8. *(v0.1: link-migráció — A7-hez tartozik, e csomagban NEM scope)* | — helyette: **A1 approve-őr** (sync előtt blokk, sync után átmegy) ✅ |
| 9. *(v0.2 bővítés)* **A2 state-őr** | ✅ megerősített `maradek_kockazat`-ot a javaslat nem írta felül (notice) |

**A9 regresszió:** E1-megerősítés (build-komponens + mező-szintű confirm), Draft→In review→Approved lánc + „Új verzió" v2-klón, 12 oldal betöltése hiba nélkül (cockpit, P0–P3, documents, requirements, process, solution, sources, goldenset, builddoc), 0 kliens-oldali pageerror. `npx tsc --noEmit` + `npm run i18n:check` zöld (1751 kulcs, HU/EN paritás), prod build zöld.

### Verifikációs leletek (javítva)
- **seeded_at-hiány új sorokon:** a 0013 csak a meglévő sorokat töltötte fel; az új seed-behúzás és az AI-struktúra-javaslat eredet-kötéses sorai `seeded_at` nélkül jöttek létre → az origin_drift sosem aktiválódott volna rájuk. Javítva (`ed2b4f0`): mindkét insert írja a bélyeget.

### Harness-megjegyzések (nem repo-kód)
- A lokális PostgREST-shim `stale_acks` táblával + generikus RPC-átjátszással bővült (a `new_artifact_version` teszthez).
- A demo-DB fázis-állapotait (P1–P3 in_progress) és a G-fázisú negatív tesztek entitás-állapot flipjeit SQL-lel állítottam — ezek teszt-előkészítések, nem az app útvonalai.

## Migrációk — Máté futtatja kézzel a Supabase SQL editorban

E csomag EGY új migrációt hozott (a korábbiak már futtatva vannak):

1. **`supabase/migrations/0013_versioning_staleness.sql`** — `input_items.group_id + version` (backfill + NOT NULL + unique(group_id,version)), `build_components.seeded_at` (backfill), `artifacts.synced_at`, `stale_acks` tábla (+RLS). Idempotens, lokálisan 2× futtatva.

⚠️ A futtatás után: `NOTIFY pgrst, 'reload schema';` (a fájl végén benne van, de ha külön részletekben futtatod, a végén egyszer mindenképp fusson le, különben a PostgREST nem látja az új oszlopokat/táblát).

## Eltérések a spectől (jelzéssel)

- A6-nál a `newVersionAction`-ra is kitettem a retired-őrt (a spec „új példány nem hozható létre" elvének kiterjesztése — az új verzió is új sor). Ha a kivezetett dokumentum új verziójának engedélyezése mégis cél, egy sor törlésével visszavehető.
- A §6 (8) link-migrációs forgatókönyv az A7-tel (kötéstábla-konszolidáció) együtt kerül ki — e csomagban helyette az A1 approve-őr és az A2 state-őr forgatókönyve fut (a v0.2 G-W-T-k szerint).

## Parkoló-lista (nem épült be, jelezve)

- A jelölők teljes vizuális nyelve + „N függő elem" upstream-nézet → **Csomag B** (spec szerint halasztott).
- A Tesztriport v2 (verzió-klón) `synced_at`-ja null-lal indul — a `new_artifact_version` RPC nem másolja; ez konzisztens (új verziónál új szinkron kell az approve-hoz), de ha másolás a cél, az RPC-t kell bővíteni (DB-migráció).
- A sources oldal olvasója mindig a legfrissebb verziót mutatja; verzió-diff nézet nincs (nem volt scope).
- A workspace ① lista a csoport-képviselőt mutatja vN jelvénnyel; a korábbi verziók ott nem listázódnak (a Források oldalon élnek).

## Cleanup

- `.env.local` törölve; a lokális next-szerver és a shim leállítva. A demo-DB a verifikáció utáni állapotban maradt (lokális, nem éles adat).
