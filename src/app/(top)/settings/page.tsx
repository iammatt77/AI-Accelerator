import { ComingSoon } from "@/components/ComingSoon";
import { IconSettings } from "@/components/icons";

export default function SettingsPage() {
  return <ComingSoon sectionKey="settings" icon={<IconSettings size={18} />} />;
}
